{
  "name": "time-capture",
  "version": "1.0.13",
  "icon": "./Images/LOGO.ico",
  "description": "",
  "main": "main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/BillBot-BoticX/Time-Capture_test.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^5.0.1",
    "electron-builder": "^20.40.2"
  },
  "dependencies": {
    "active-windows": "^0.1.14",
    "electron-updater": "^4.3.5",
    "windows-active-process": "^1.1.1"
  }
}
